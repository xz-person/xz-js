export const PROJECT_PREFIX = 'xz-';

export const CWD = process.cwd();

/**
 * 配置文件名称
 */
export const CONFIG_FILE = 'deps.config.ts';

/**
 * 临时目录名称（存放依赖源项目）
 */
export const TEMP = '.temp';

/**
 * 依赖目录名称（存放构建后的依赖项目）
 */
export const DEPS = 'deps';
